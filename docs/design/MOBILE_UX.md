# Mobile UX

Factory-floor flow is scan → product card → action → submit → confirmation. The default is a soft light industrial surface with clear separators and dark text. Current user and machining preference remain visible without repeated prompts. Product/QR fields are clearly separated from live server fields. QC, shortage, NG/rework, packing, delivery and report actions are grouped by mode; forms request only quantity and relevant notes.
